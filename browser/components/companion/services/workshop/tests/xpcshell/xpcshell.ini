[DEFAULT]
firefox-appdir = browser
head = head_workshop.js
# These xpcshell tests should never be run under verify mode because this
# implies reuse of the same underlying profile storage and we don't currently
# support / want that.  (We could clear the origin at the end of the test, but
# there's no real need to do that.)
skip-if =
  verify

# Note: Although things would go faster if we set `prefs` of
# `browser.tabs.remote.autostart=false`, the value gets sampled and latched in
# xpcshell before the preferences specified can take effect, so we no longer
# list it here, as it's just confusing.

[test_calendar_links.js]
[test_calendar_search_all.js]
[test_calendar_single_day.js]
[test_calendar_recurring_events.js]
[test_calendar_attendees.js]
[test_calendar_cleanup.js]
[test_calendar_moving_event_with_title.js]
[test_calendar_owner.js]
[test_feed_parsers.js]
support-files =
  bootstrap_workshop.js
  test_rss.xml
  test_rss_expected.json
  test_atom.xml
  test_atom_expected.json
  test_hfeed.html
  test_hfeed_expected.json
  test_jsonfeed.json
  test_jsonfeed_expected.json
  test_gmail_feed.xml
  test_gmail_feed_expected.json
[test_feed_accounts.js]
[test_account_properties.js]
support-files =
  test_gmail_feed.xml
[test_token_revocation.js]
[test_account_creation.js]
